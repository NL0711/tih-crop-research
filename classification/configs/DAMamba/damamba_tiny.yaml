MODEL:
  TYPE: DAMamba
  NAME: tiny
  DROP_PATH_RATE: 0.15
  DAMAMBA:
    EMBED_DIM: [80, 160, 320, 512]
    DEPTHS: [3, 4, 12, 5]
    MLP_RATIO: (4, 4, 3, 3)
    HEAD_DIM: 16
    LAYERSCALE: [False,False,False,False]
    USE_ATTENTION: 
    USE_SE_ONLY: True
  DDP: 'torch'

TRAIN:
  EPOCHS: 10
  WARMUP_EPOCHS: 1
  MESA: 1.0
