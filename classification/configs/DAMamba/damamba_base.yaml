MODEL:
  TYPE: DAMamba
  NAME: base
  DROP_PATH_RATE: 0.5
  DAMAMBA:
    EMBED_DIM: [112, 224, 448, 640]
    DEPTHS: [4, 8, 25, 8]
    MLP_RATIO: (4, 4, 3, 4)
    HEAD_DIM: 16
    LAYERSCALE: [False,False,True,True]
    USE_ATTENTION: False
    USE_SE_ONLY: False
  DDP: 'torch'

TRAIN:
  EPOCHS: 50 #changed from 300
  WARMUP_EPOCHS: 3
  MESA: 2.0
